{{#if plugin.supportCategory}}
  {{d-button
    href=plugin.supportCategory.url
    title="plugin_manager.plugin.issues.title"
    label=issueLabel
    icon="far-life-ring"
    class=issuesClass}}
{{/if}}
{{#if plugin.documentationCategory}}
  {{d-button
    href=plugin.documentationCategory.url
    title="plugin_manager.plugin.documentation.title"
    label=documentationLabel
    icon="book"
    class=documentationClass}}
{{/if}}
